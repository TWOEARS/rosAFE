CPP = g++
C = gcc 
CFLAGS = -Wall -g -O -std=c++11 -DHAVE_INLINE

all:
	$(CPP) $(CFLAGS) ../../src/TimeDomainSignal.hpp testTimeDomain.cpp -o runTestTimeDomain

rebuild: uninstall clean all	

uninstall:
	rm -rf run

clean:
	rm -f *.o all
