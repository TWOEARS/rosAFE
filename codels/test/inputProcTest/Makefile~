CPP = g++
C = gcc 
CFLAGS = -Wall -g -O -std=c++11 -lpthread -DHAVE_INLINE

all:
	$(CPP) $(CFLAGS) inputProcTests.cpp -o runInputProcTest

rebuild: uninstall clean all	

uninstall:
	rm -rf run

clean:
	rm -f *.o all
