CPP = g++
C = gcc 
CFLAGS = -Wall -g -O -std=c++11 -DHAVE_INLINE

all:
	$(CPP) $(CFLAGS) ../../src/Signal.hpp signalTest.cpp -o runTestSignal

rebuild: uninstall clean all	

uninstall:
	rm -rf run

clean:
	rm -f *.o all
